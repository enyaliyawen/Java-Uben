package com.enyaliyawen.collection;

public class TestLinkedHashSet {

	/*
	 * 1.LinkedHashSet是HashSet的子类
	 * 2. LinkedHashset集合根据元素的hasdCode值来决定元素的存储位置，但它同时使用链表维护元素的次序，这使得元素看起来是以插入顺序保存的；
	 * 3. LikedHasSet性能插入性能略低于HashSet,但在迭代访问Set里的全部元素时有很好的性能。
	 * 4.LinkedHashSet不允许集合元素重复；
	 */
}
